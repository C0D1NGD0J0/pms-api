<table width="100%">
  <tr>
    <td class="content">
      <h2>Your Resident Portal Access is Ready</h2>
      <p>Hi <%= inviteeName %>,</p>
      <p>
        <%= inviterName %> from <strong><%= companyName %></strong> has set up your resident portal account.
      </p>
      <p>
        This gives you 24/7 access to submit maintenance requests, pay rent online, and communicate with your property management team.
      </p>
      <p>
        Everything you need to manage your residency is now available at your fingertips.
      </p>

      <% if (customMessage) { %>
      <div style="background-color: #f8f9fa; padding: 15px; border-left: 4px solid #007bff; margin: 20px 0;">
        <p style="margin: 0; font-style: italic;">
          "<%= customMessage %>"
        </p>
      </div>
      <% } %>

      <p>
        Click below to activate your account and get started.
      </p>
      <p>
        You'll be able to access all your resident services immediately after setup.
      </p>
    </td>
  </tr>

  <tr>
    <td class="content cta">
      <a href="<%= invitationUrl %>" class="btn">Activate My Account</a>
      <p style="font-size: 14px; color: #666; margin-top: 15px">
        If the button doesn't work, copy and paste this link in your browser:<br />
        <%= invitationUrl %>
      </p>
      <p style="font-size: 12px; color: #999">
        This invitation will expire on <%= new Date(expiresAt).toLocaleDateString() %> at <%= new Date(expiresAt).toLocaleTimeString() %>.
        <br />
        If you didn't expect this invitation, you can safely ignore this email.
      </p>
    </td>
  </tr>
</table>
