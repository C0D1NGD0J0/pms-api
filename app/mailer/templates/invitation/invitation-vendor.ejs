<table width="100%">
  <tr>
    <td class="content">
      <h2>Work Opportunities Available with <%= companyName %></h2>
      <p>Hi <%= inviteeName %>,</p>
      <p>
        <%= inviterName %> from <strong><%= companyName %></strong> would like to invite you to join our network of trusted service providers.
      </p>
      <p>
        We have regular work opportunities available and think you'd be a great fit for our property maintenance needs.
      </p>
      <p>
        You'll be able to receive work orders, submit invoices, and get paid efficiently through our platform.
      </p>

      <% if (customMessage) { %>
      <div style="background-color: #f8f9fa; padding: 15px; border-left: 4px solid #007bff; margin: 20px 0;">
        <p style="margin: 0; font-style: italic;">
          "<%= customMessage %>"
        </p>
      </div>
      <% } %>

      <p>
        Ready to get started? Click below to set up your account.
      </p>
      <p>
        It only takes a few minutes to complete your profile and start receiving work requests.
      </p>
    </td>
  </tr>

  <tr>
    <td class="content cta">
      <a href="<%= invitationUrl %>" class="btn">Accept Invitation</a>
      <p style="font-size: 14px; color: #666; margin-top: 15px">
        If the button doesn't work, copy and paste this link in your browser:<br />
        <%= invitationUrl %>
      </p>
      <p style="font-size: 12px; color: #999">
        This invitation will expire on <%= new Date(expiresAt).toLocaleDateString() %> at <%= new Date(expiresAt).toLocaleTimeString() %>.
        <br />
        If you didn't expect this invitation, you can safely ignore this email.
      </p>
    </td>
  </tr>
</table>
