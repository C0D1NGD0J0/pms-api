You've Been Invited to PropertyDesk
=====================================

Hi <%= inviteeName %>,

<%= inviterName %> has invited you to join <%= companyName %> on PropertyDesk as a <%= role %>.

<% if (customMessage) { %>
Message from <%= inviterName %>:
"<%= customMessage %>"

<% } %>
To accept this invitation, visit:
<%= invitationUrl %>

This invitation expires on <%= new Date(expiresAt).toLocaleDateString() %>.

Next steps after accepting:
- Create your account or sign in if you already have one
- Complete your profile setup
- Start collaborating with the <%= companyName %> team

If you weren't expecting this invitation, you can safely ignore this email.

