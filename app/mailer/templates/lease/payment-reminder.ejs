<table role="presentation" width="100%">
  <tr>
    <td class="content">
      <p>Hi <strong><%= tenantName %></strong>,</p>
      <p>
        Your rent payment for <strong><%= propertyAddress %></strong> is due soon.
      </p>
    </td>
  </tr>

  <tr>
    <td class="content" style="padding-top: 0;">
      <%- include("../shared/html/_detail-table", { rows: [
        { label: "Property", value: propertyAddress },
        ...(unitNumber ? [{ label: "Unit", value: unitNumber }] : []),
        { label: "Amount Due", value: '<span style="font-size: 20px; font-weight: 700; color: #062f4f;">' + amountDue + '</span>' },
        { label: "Due Date", value: '<span style="color: #dc2626; font-weight: 600;">' + new Date(dueDate).toLocaleDateString() + '</span>' },
        { label: "Days Until Due", value: daysUntilDue + ' days' }
      ]}) %>
    </td>
  </tr>

  <% if (paymentUrl) { %>
  <tr>
    <td class="cta">
      <%- include("../shared/html/_button", { url: paymentUrl, label: "Pay Now", colorClass: "btn-success" }) %>
    </td>
  </tr>
  <% } %>

  <tr>
    <td class="content">
      <h3>Payment Methods</h3>
      <ul>
        <% if (paymentMethods && paymentMethods.includes('online')) { %>
        <li><strong>Online:</strong> Log in to your resident portal and pay securely</li>
        <% } %>
        <% if (paymentMethods && paymentMethods.includes('check')) { %>
        <li><strong>Check:</strong> Mail to <%= mailAddress %></li>
        <% } %>
        <% if (paymentMethods && paymentMethods.includes('bank')) { %>
        <li><strong>Bank Transfer:</strong> <%= bankDetails %></li>
        <% } %>
        <% if (paymentMethods && paymentMethods.includes('cash')) { %>
        <li><strong>In Person:</strong> Visit the property management office</li>
        <% } %>
      </ul>

      <%- include("../shared/html/_notice", { text: '<strong>Late Fee:</strong> A fee of <strong>' + lateFeeAmount + '</strong> will be charged if payment is not received by <strong>' + new Date(lateFeeDate).toLocaleDateString() + '</strong>.', type: 'warning' }) %>

      <% if (currentBalance && currentBalance !== amountDue) { %>
      <%- include("../shared/html/_notice", { text: '<strong>Outstanding Balance:</strong> You have an outstanding balance of <strong>' + currentBalance + '</strong>. Please contact your property manager to discuss payment arrangements.', type: 'danger' }) %>
      <% } %>

      <p style="font-size: 14px; color: #64748b; margin-top: 20px;">
        If you've already made this payment, please disregard this reminder. For questions or payment arrangements, contact your property manager at
        <a href="mailto:<%= propertyManagerEmail %>"><%= propertyManagerEmail %></a> or <%= propertyManagerPhone %>.
      </p>
    </td>
  </tr>
</table>
