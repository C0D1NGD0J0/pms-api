<table role="presentation" width="100%">
  <tr>
    <td class="content">
      <p>
        A maintenance charge has been added to your account for work completed at your property.
        You have until <strong><%= new Date(dueDate).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %></strong> to pay manually —
        after that date, the amount will be automatically debited from your payment method on file.
      </p>
    </td>
  </tr>

  <tr>
    <td class="content" style="padding-top: 0;">
      <%
        var _rows = [
          { label: 'Reference #', value: mruid }
        ];
        if (jobTitle) {
          _rows.push({ label: 'Job', value: jobTitle });
        }
        _rows.push({ label: 'Amount Due', value: '$' + (amountInCents / 100).toFixed(2) + ' ' + (currency || 'USD').toUpperCase() });
        _rows.push({ label: 'Due Date', value: new Date(dueDate).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) });
      %>
      <%- include("../shared/html/_detail-table", { rows: _rows, variant: 'amber' }) %>
    </td>
  </tr>

  <tr>
    <td class="content" style="text-align: center; padding: 24px 32px;">
      <%- include("../shared/html/_button", { url: process.env.APP_URL + '/tenants/' + cuid + '/' + pytuid + '/payments', label: 'Pay Now', colorClass: 'btn-accent' }) %>
    </td>
  </tr>

  <tr>
    <td class="content">
      <p style="font-size: 13px; color: #64748b;">
        If you do not pay by the due date, the amount will be automatically charged to your payment method on file.
        Questions? Contact your property manager directly.
      </p>
    </td>
  </tr>
</table>
