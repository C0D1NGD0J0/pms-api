<table role="presentation" width="100%">
  <tr>
    <td class="content">
      <p>
        A payout has been <strong style="color: #16a34a;">initiated</strong> for the maintenance job below. Funds will arrive in your bank account according to your payout schedule.
      </p>
    </td>
  </tr>

  <tr>
    <td class="content" style="padding-top: 0;">
      <%
        var _rows = [
          { label: 'Reference #', value: mruid }
        ];
        if (jobTitle) {
          _rows.push({ label: 'Job', value: jobTitle });
        }
        _rows.push({ label: 'Payout Amount', value: '$' + (amountInCents / 100).toFixed(2) + ' ' + currency.toUpperCase() });
        _rows.push({ label: 'Transfer ID', value: transferId });
        _rows.push({ label: 'Date', value: new Date().toLocaleDateString() });
      %>
      <%- include("../shared/html/_detail-table", { rows: _rows }) %>
    </td>
  </tr>

  <tr>
    <td class="content">
      <p style="font-size: 14px; color: #64748b;">
        Funds typically arrive within 1-2 business days depending on your bank and payout schedule. You can check your payout status in your account dashboard.
      </p>
    </td>
  </tr>
</table>
