Your Account is Ready
=====================

Hi <%= firstName %>,

Your PropertyDesk account at <%= companyName %> has been set up.

<% if (role === ROLES.VENDOR) { %>
As a vendor partner, you have access to the vendor portal to receive work orders, submit invoices, and coordinate with the team.
<% if (vendorCompanyName) { %>Your company <%= vendorCompanyName %> is registered in our system.<% } %>
<% } else if (role === ROLES.TENANT) { %>
As a resident, your portal gives you 24/7 access to submit maintenance requests, pay rent, and communicate with your property management team.
<% } else { %>
As a <%= role %> team member, you can manage properties, work with residents and vendors, and collaborate with your team.
<% if (department) { %>You've been assigned to the <%= department %> department<% if (jobTitle) { %> as <%= jobTitle %><% } %>.<% } %>
<% } %>

<% if (customMessage) { %>
A message from your team:
"<%= customMessage %>"

<% } %>
YOUR LOGIN CREDENTIALS
----------------------
Email:              <%= email %>
Temporary Password: <%= temporaryPassword %>

Please change your password immediately after your first login.

<% if (expectedStartDate) { %>
Your expected start date is <%= new Date(expectedStartDate).toLocaleDateString() %>.
<% } %>

Log in here: <%= loginUrl %>

If you have any questions, contact your administrator.

