[build]
builder = "nixpacks"
buildCommand = "npm install --production && npm install typescript tsc-alias --no-save && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[env]
NODE_ENV = "production"